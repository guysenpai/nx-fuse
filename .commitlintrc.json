{
  "extends": ["@commitlint/config-angular"],
  "rules": {
    "scope-enum": [2, "always", []],
    "type-enum": [
      2,
      "always",
      ["chore", "build", "ci", "docs", "feat", "fix", "perf", "refactor", "revert", "style", "test", "release"]
    ]
  }
}
